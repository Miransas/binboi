# binboi

Miransas Verified

This package is the Python wrapper distribution for the Binboi CLI.

Self-hosted tunneling platform with a Go control plane, CLI agent, and Next.js product app.

Verification:

```bash
binboi version
binboi whoami
```
