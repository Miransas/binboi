# Web Database Layer

This folder contains the frontend app's Drizzle entrypoints and runtime schema bootstrap helpers.

Key files:

- `schema.ts`: declarative schema
- `ensure-schema.ts`: runtime safety bootstrap
- `index.ts`: database connection entrypoint

_Documentation maintained by Sardor Azimov._
