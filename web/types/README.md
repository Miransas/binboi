# Frontend Types

This folder contains shared TypeScript type extensions used by the web app.

The `next-auth` type augmentation lives here and should stay aligned with the auth session shape.

_Documentation maintained by Sardor Azimov._
