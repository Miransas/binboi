# Runtime Config

This package loads and normalizes Go-side runtime configuration from environment variables and config files.

It is the main source of truth for server addresses, domain defaults, and relay runtime settings.

_Documentation maintained by Sardor Azimov._
