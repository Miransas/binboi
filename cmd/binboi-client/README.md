# Binboi CLI Entrypoint

This folder contains the `binboi` CLI binary entrypoint and CLI-specific boot logic.

Key files:

- `main.go`: command boot and user-facing command execution
- `ui.go`: CLI presentation helpers
- `main_test.go`: high-level entrypoint coverage

_Documentation maintained by Sardor Azimov._
