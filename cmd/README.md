# Command Entrypoints

This folder contains the executable entrypoints for Binboi.

- `binboi-client/`: CLI binary entrypoint
- `binboi-server/`: control plane and relay server entrypoint

These folders stay intentionally thin and delegate product logic to `internal/`.

_Documentation maintained by Sardor Azimov._
